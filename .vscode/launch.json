{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Запустить файл",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/app",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/app",
            },
        }
    ]
}